package authentication

// login
// currently only supports username or email, and password

func Login(loginRequest *LoginRequest) {
	// check if the login request is valid
	// if not, return an error
	// if valid, generate a token and return it
}

// register

func Register(registrationRequest *RegistrationRequest) {
	// check if the registration request is valid
	// if not, return an error
	// if valid, create a user and return a token
}
